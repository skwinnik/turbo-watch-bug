{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*", "$TURBO_ROOT$/external.json"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
  }
}